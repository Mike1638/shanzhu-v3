import { Bar } from "../../views/Bar";
import { Foo } from "../../views/Foo";

export const routes = [
    {
        path: '/foo', component: Foo
    },
    {
        path: '/bar', component: Bar
    }
]